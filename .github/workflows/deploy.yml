# name: Trigger Jenkins Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   trigger-jenkins:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Trigger Jenkins Build
#         run: |
#           curl -X POST \
#             --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}" \
#             "${{ secrets.JENKINS_URL }}/job/deploy-project/build?token=${{ secrets.JENKINS_JOB_TOKEN }}"

#       - name: Wait and Check Status
#         run: echo "Jenkins pipeline triggered. Monitor at ${{ secrets.JENKINS_URL }}"